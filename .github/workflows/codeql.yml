name: "CodeQL"

on:
  workflow_dispatch:
  schedule:
    - cron: '30 21 * * *'

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: [self-hosted, linux, xlarge, x64]
    permissions:
      security-events: write
      actions: read
      contents: read

    container:
      image: vesoft/nebula-dev:ubuntu2004
      options: --cap-add=SYS_PTRACE

    strategy:
      fail-fast: false
      matrix:
        language: [ 'cpp' ]

    steps:
    - uses: webiny/action-post-run@3.1.0
      with:
        run: sh -c "find . -mindepth 1 -delete"
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}

    - name: Build project
      run: |
        [ -d build/ ] && rm -rf build/* || mkdir -p build && cd build
        cmake \
          -DCOMPILER_LAUNCHER_OPTION=sccache \
          -DCMAKE_CXX_COMPILER=$TOOLSET_GCC_DIR/bin/g++ \
          -DCMAKE_C_COMPILER=$TOOLSET_GCC_DIR/bin/gcc \
          -DNEBULA_USE_LINKER=mold \
          -DCMAKE_BUILD_TYPE=Release \
          -DENABLE_TESTING=on \
          -GNinja \
          ..
        ninja -j $(nproc)

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"
